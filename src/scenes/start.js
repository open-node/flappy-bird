function Start(game) {
  // update 更新个成员
  const update = () => {};

  // 渲染各成员
  const render = () => {};

  // 进入场景
  const enter = () => {};

  return { update, render, enter };
}

module.exports = Start;
